const { Schema, model } = require('Mongoose');

// Schema to create thoughts model
const thoughtSchema = new Schema(
            {
            type: String,
            required: true,
            min:
            max:
        },
